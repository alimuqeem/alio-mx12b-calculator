// No privileged APIs are exposed; the renderer is fully self-contained
// (pure DOM + Web Audio API), so this file intentionally stays empty
// aside from context isolation being enforced in main.js.
